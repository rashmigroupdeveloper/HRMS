{
  "name": "hrms-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "knip": "knip",
    "depcruise": "depcruise src --config .dependency-cruiser.cjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "verify": "npm run typecheck && npm run lint && npm run knip && npm run depcruise && npm run test && npm run build",
    "format": "prettier --write .",
    "migrate": "node-pg-migrate --tsconfig tsconfig.json -j ts --ignore-pattern \".*\\.md\" up",
    "migrate:down": "node-pg-migrate --tsconfig tsconfig.json -j ts --ignore-pattern \".*\\.md\" down",
    "migrate:create": "node-pg-migrate --tsconfig tsconfig.json -j ts --ignore-pattern \".*\\.md\" create"
  },
  "dependencies": {
    "@orpc/openapi": "^1.14.7",
    "@orpc/server": "^1.14.7",
    "@orpc/zod": "^1.14.7",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "kysely": "^0.28.2",
    "pg": "^8.16.0",
    "pino": "^9.7.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.30.0",
    "@types/express": "^5.0.0",
    "@types/node": "^22.15.0",
    "@types/pg": "^8.15.0",
    "@types/supertest": "^6.0.3",
    "dependency-cruiser": "^16.10.0",
    "eslint": "^9.30.0",
    "knip": "^5.61.0",
    "node-pg-migrate": "^7.9.0",
    "prettier": "^3.6.0",
    "supertest": "^7.1.0",
    "ts-node": "^10.9.2",
    "tsx": "^4.20.0",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.35.0",
    "vitest": "^3.2.0"
  }
}
