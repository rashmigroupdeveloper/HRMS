/**
 * Kysely database interface — the single source of table types for queries.
 *
 * Deliberately EMPTY at setup (Stage 0.0): tables land here alongside their
 * migrations from Stage 0.4 onward (auth/RBAC first, per docs/03 §1).
 */
// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface Database {}
